{
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -c ./js/tailwind.config.js -i ./css/dashboard-tailwind.css -o ./css/dashboard.tailwind.css --minify",
    "build": "node scripts/generate-config.mjs && npm run build:css"
  },
  "devDependencies": {
    "tailwindcss": "3.4.17"
  }
}
